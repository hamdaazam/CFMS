import { UnifiedProfile } from '../components/features/profile/UnifiedProfile';

export default function HODProfile() {
  return <UnifiedProfile role="hod" />;
}

// Removed the previous implementation and types - HOD uses UnifiedProfile wrapper only
